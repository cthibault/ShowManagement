<Window x:Class="WpfTest.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <StackPanel Orientation="Vertical">
            <Label>Make</Label>
            <ComboBox ItemsSource="{Binding Vehicles}" DisplayMemberPath="Key" IsSynchronizedWithCurrentItem="True" />

            <Label Content="{Binding Path=Key}" />
            <ComboBox ItemsSource="{Binding Vehicles/Value}" DisplayMemberPath="Name" />
        </StackPanel>
        <!--<StackPanel Orientation="Vertical">
            <Label>Make</Label>
            <ComboBox ItemsSource="{Binding}" DisplayMemberPath="Key" IsSynchronizedWithCurrentItem="True" />

            <Label Content="{Binding Path=Key}" />
            <ComboBox ItemsSource="{Binding Path=Value}" DisplayMemberPath="Name" />
        </StackPanel>-->
    </Grid>
</Window>
